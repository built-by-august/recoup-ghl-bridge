{
  "name": "recoup-ghl-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recoup → GoHighLevel webhook bridge. Receives audit widget submissions and syncs contacts, opportunities, and notes to GHL via Private Integration Token.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
